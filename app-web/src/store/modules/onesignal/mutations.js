
export default {
  setNotificationPermission: (state, notificationPermission) => state.notification_permission = notificationPermission,
  setNotificationSupported: (state, notificationSupported) => state.notification_supported = notificationSupported
}
