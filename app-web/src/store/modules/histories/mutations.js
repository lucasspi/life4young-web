
export default {
  setRows(state, rows) {
    state.rows = rows;
  },
  setDelay(state, delay) {
    state.delay = delay;
  },
  setRefreshInterval(state, refreshInterval) {
    state.refreshInterval = refreshInterval;
  }
}
